import { LoginButton } from "@/components";
import { Box } from "@mui/material"

const Page = async() => {
    return(
        <Box>
            <LoginButton />
        </Box>
    )
}

export default Page;