/** @type {import('next').NextConfig} */
const nextConfig = {
    // webpack: (config) => {
    //     config.resolve.fallback = {
    //       fs: false,
    //       child_process: false,
    //       net: false,
    //       tls: false,
    //       // http2: false
    //     };
    
    //     return config;
    //   },
};

export default nextConfig;
